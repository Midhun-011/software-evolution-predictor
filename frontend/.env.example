# Frontend environment variables
# During development, the Vite proxy handles API requests to localhost:4000
# For production, set the full API URL

# API URL (only needed for production)
# VITE_API_URL=https://your-api-domain.com
